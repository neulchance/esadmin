```ts
KeyMod.CtrlCmd | KeyCode.KeyW
KeybindingsRegistry.registerKeybindingRule()
td/workbench/electron-sandbox/desktop.contribution.ts:50:8
위 경로의 코드에서는 cmd+w 키조합으로 등록된 Action CloseWindowAction 을 실행 되도록 한다.
개발편의를 위해 현재는 주석처리 해 둔다.
```